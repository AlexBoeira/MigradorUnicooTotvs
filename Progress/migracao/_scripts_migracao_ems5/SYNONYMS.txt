SELECT 'CREATE SYNONYM ' || OBJECT_NAME || ' FOR  ' || OWNER || '.' ||
       OBJECT_NAME || '@' || '&DBLINK;' COMANDO
  FROM ALL_OBJECTS
 WHERE OBJECT_TYPE IN ('TABLE', 'FUNCTION')
   AND OWNER IN ('PRODUCAO', 'EMS506UNICOO')
